# 🖥️ PhiDOS: نظام تشغيل Phi-Chain المبني على مبادئ AMSDOS

نظام **PhiDOS** هو بيئة تشغيلية مصممة لتعمل فوق طبقة Phi-Chain، مستلهمة بساطة وكفاءة نظام **AMSDOS** (نظام تشغيل Amstrad) ولكن مع دمج خصائص البلوكشين والنسبة الذهبية.

## 1. المبادئ الأساسية (Core Principles)

*   **البساطة (Simplicity):** واجهة سطر أوامر (CLI) مباشرة تشبه AMSDOS.
*   **الحتمية (Determinism):** جميع العمليات تعتمد على حسابات `PhiMath` لضمان التوافق.
*   **التكامل مع البلوكشين:** الملفات ليست مجرد بيانات على القرص، بل هي **Entangled Data Objects (EDOs)** مسجلة في السلسلة.

## 2. هيكلية نظام الملفات (Phi-FileSystem)

بدلاً من جدول تخصيص الملفات التقليدي (FAT)، يستخدم PhiDOS نظام **Phi-FAT**:
*   **العناوين:** يتم حساب مواقع البيانات باستخدام متتالية فيبوناتشي.
*   **التحقق:** كل ملف له "بصمة تماسك" (Coherence Hash) تربط بين حالته الرقمية والفيزيائية.

## 3. إدارة الذاكرة (Phi-Memory)

*   يتم تقسيم الذاكرة إلى خلايا زمنية (Temporal Cells) تعتمد على النسبة الذهبية لتقليل التجزئة (Fragmentation).

## 4. الأوامر الأساسية (Basic Commands)

| الأمر | الوظيفة | المقابل في AMSDOS |
| :--- | :--- | :--- |
| `PHI-DIR` | عرض الملفات (EDOs) المسجلة | `DIR` |
| `PHI-SAVE` | حفظ ملف وتعميته في البلوكشين | `SAVE` |
| `PHI-LOAD` | استرجاع ملف والتحقق من تماسك | `LOAD` |
| `PHI-RUN` | تنفيذ عقد ذكي أو برنامج Phi | `RUN` |

---
**ملاحظة:** PhiDOS ليس مجرد محاكي، بل هو واجهة التفاعل الأساسية مع نظام Phi-Chain البيئي.
